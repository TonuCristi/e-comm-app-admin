import { HiArrowSmallRight } from "react-icons/hi2";
import styled from "styled-components";

const StyledToPathIcon = styled(HiArrowSmallRight)`
  font-size: 2.4rem;
  stroke-width: 1;
`;

export default function ToPathIcon() {
  return <StyledToPathIcon />;
}
