import { HiMiniArrowSmallDown } from "react-icons/hi2";
import styled from "styled-components";

const StyledUpdateIcon = styled(HiMiniArrowSmallDown)`
  font-size: 2.4rem;
  stroke-width: 1;
`;

export default function UpdateIcon() {
  return <StyledUpdateIcon />;
}
